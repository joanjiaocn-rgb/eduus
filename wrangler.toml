name = "eduus"
compatibility_date = "2024-01-01"

[env.production]

# Environment variables - For production, set these in Cloudflare Dashboard
[vars]
# OPENAI_API_KEY = "your-api-key-here"  # Set via Cloudflare Dashboard > Pages > Project Settings > Environment Variables

# Cloudflare Pages configuration
[site]
bucket = ".vercel/output/static"
